# Item 2 activity scenario matrix

Source-review scenario specifications only; these cases have not been executed.

| Kind | Activity | Configuration/refusal case | Recovery/reset case | Evidence case |
|---|---|---|---|---|
| lab | `intro-filtration-demo` | Attempt one scientifically invalid or out-of-sequence operation specific to Precipitate and Filter Calcium Carbonate and verify the shared/custom runtime rejects it without committing evidence. | If filtration is attempted before the funnel/filter paper and receiver are correctly staged, correct that apparatus state, repeat only the rejected transfer/filtration step, then restart the demo and verify the previous filtrate/precipitate records are gone. | Require the recorded sample-volume evidence, successful calcium-carbonate precipitation, filtration completion, and distinct filtrate/precipitate state needed by the demo's terminal validations. |
| lab | `hard-water-demo` | Attempt one scientifically invalid or out-of-sequence operation specific to Hard-Water Reference Demo and verify the shared/custom runtime rejects it without committing evidence. | If the gravimetric path reaches weighing before the precipitate is properly filtered/dried, return to filtration or drying as indicated, cool and reweigh, then reset the demo and confirm prior mass/hardness records do not survive. | Require sample-volume evidence, precipitate formation/filtration, final dry-mass evidence, and the hardness calculation tied to those named measurements. |
| lab | `acid-base-titration` | Start without or with an invalid titration-form setup and verify the activity refuses before composition/play; then supply a valid instructor-approved setup and confirm only that configured path becomes available. | If titration is rejected for missing preparation/readings or an unacceptable endpoint path, correct the burette/analyte state, reacquire the required reading or restart the trial, then reset and verify no old endpoint or molarity calculation remains. | Require preparation evidence, separate initial/final burette readings, accepted endpoint evidence, and the acid-molarity calculation derived from delivered volume rather than an authored result. |
| lab | `marble-statue-kinetics` | Attempt one scientifically invalid or out-of-sequence operation specific to How Long Will That Marble Statue Last? and verify the shared/custom runtime rejects it without committing evidence. | If a kinetics trial is rejected because the marble mass, acid preparation, gas-syringe connection, or timed series is incomplete, repair that trial setup and reacquire its series; on restart, confirm the prior gas-volume series/rate is cleared. | Require measured marble mass, configured acid trial identity, a recorded gas-volume time series for each selected trial, and initial-rate calculations/comparison from those series. |
| lab | `paper-chromatography` | Start without or with an invalid paper-form setup and verify the activity refuses before composition/play; then supply a valid instructor-approved setup and confirm only that configured path becomes available. | If the origin is not above the solvent, the selected solvent/data set is unsupported, or the chamber procedure is invalid, revise the instructor setup or prepare a fresh paper/chamber and rerun that trial; reset must discard its old band/front/Rf evidence. | Require selected-solvent/procedure records, solvent-front and band-distance measurements for each approved trial, Rf calculations from those distances, and the configured disposal record. |
| lab | `equilibrium-rainbow-display` | Attempt one scientifically invalid or out-of-sequence operation specific to Equilibrium Rainbow Display and verify the shared/custom runtime rejects it without committing evidence. | If an unsupported or out-of-sequence stress is selected, restore the mixture to the supported baseline/previous node, apply one approved stress, rerecord the observation, and verify a reset removes the prior stressed-state evidence. | Require baseline/stress identity, qualitative before/after observation evidence for each approved stress, and the learner's equilibrium interpretation without substituting expected colors. |
| lab | `hand-warmer-calorimetry` | Attempt one scientifically invalid or out-of-sequence operation specific to Designing an Effective Hand Warmer and verify the shared/custom runtime rejects it without committing evidence. | If required teacher configuration, mass, timer, or temperature evidence is missing, return to the exact acquisition/configuration step rather than substituting a value, repeat the affected calorimetry segment, and reset without carrying the old calibration/design calculations. | Require balance/timing/temperature evidence, calorimeter calibration calculations, trial calorimetry calculations, teacher waste configuration, and the evidence-backed design decision. |
| lab | `blue1-spectroscopy` | Attempt one scientifically invalid or out-of-sequence operation specific to Spectroscopy of Blue #1 Dye in a Sports Drink and verify the shared/custom runtime rejects it without committing evidence. | If a photometer reading is attempted before wavelength/zero/cuvette state is valid, complete the missing calibration/blank/insertion steps and reread the same sample; restarting must clear the old %T/absorbance/calibration evidence. | Require standard/dilution preparation evidence, valid instrument zero/wavelength/cuvette reads, recorded %T/absorbance values, accepted class calibration, and the unknown result derived from that calibration. |
| lab | `brass-colorimetry` | Attempt one scientifically invalid or out-of-sequence operation specific to How Can Color Determine the Copper Content of Brass? and verify the shared/custom runtime rejects it without committing evidence. | If a scan or unknown read is rejected for missing dark/blank zero, wavelength configuration, or cuvette state, restore the instrument sequence and reacquire that wavelength/sample; reset must remove the old scan/calibration/unknown result. | Require the wavelength-scan read/record series under valid zero state, approved-wavelength calibration evidence, prepared unknown absorbance, and the resulting concentration/copper calculation. |
| lab | `hard-water-analysis` | Start without or with an invalid hard-water-form setup and verify the activity refuses before composition/play; then supply a valid instructor-approved setup and confirm only that configured path becomes available. | If instructor drying/cooling settings are invalid or a sample lacks the required filtered/dried mass evidence, correct the setup or repeat the affected dry-cool-weigh stage for that sample, then restart without retaining either sample's prior hardness/uncertainty values. | Require practice preparation, filtration/drying evidence under instructor settings, sample-specific volume and dry-mass evidence for both waters, and paired hardness/uncertainty calculations. |
| lab | `beverage-acidity` | Start without or with an invalid titration-form setup and verify the activity refuses before composition/play; then supply a valid instructor-approved setup and confirm only that configured path becomes available. | If the teacher proxy concentrations/aliquot/NaOH setting is invalid or a pH-volume trial lacks required preparation/evidence, correct the setup and rerun that beverage trial from its valid start; reset must clear its old curve/equivalence/acidity result. | Require teacher configuration identity, beverage aliquot evidence, recorded pH-volume/burette rows, accepted equivalence/endpoint evidence, and acidity calculations from each selected beverage trial. |
| lab | `bonding-unknown-solids` | Start without or with an invalid bonding-form setup and verify the activity refuses before composition/play; then supply a valid instructor-approved setup and confirm only that configured path becomes available. | If a selected test is outside the approved panel/order, lacks the required prepared test solution, or risks revealing a blind identity, return to the approved test step for that sample and rerecord it; reset must preserve blindness while clearing prior test/classification evidence. | Require the host-selected test panel evidence per known/blind sample, preserve blind-redacted identity in those records, and require classification reasoning to consume the recorded observations rather than hidden labels. |
| lab | `green-chemistry-mixture-purification` | Start without or with an invalid custom-player setup and verify the activity refuses before composition/play; then supply a valid instructor-approved setup and confirm only that configured path becomes available. | If the selected tare convention/setup is inconsistent or constant-mass/recovery evidence is incomplete, return to the appropriate tare/heating/cooling/recovery step under the same approved convention, reacquire masses, and restart without cross-contaminating the alternate tare witness. | Require approved setup/tare-convention identity, starting/tare/heating-cooling mass evidence, recovered-fraction masses, and atom-economy/recovery/green-chemistry calculations tied to those records. |
| lab | `hydrogen-peroxide-redox-titration` | Start without or with an invalid titration-form setup and verify the activity refuses before composition/play; then supply a valid instructor-approved setup and confirm only that configured path becomes available. | If sample analysis is attempted before titrant standardization or replicate concordance fails, complete/repeat the standardization replicate set first, then rerun the affected sample titration; reset must clear stale standardized-titrant and sample-percent calculations. | Require accepted standardization replicate calculations/concordance before sample work, sample aliquot and separate burette readings, and peroxide percent/concentration calculations using the standardized titrant record. |
| lab | `quick-ache-relief-separation` | Start without or with an invalid quick-ache-form setup and verify the activity refuses before composition/play; then supply a valid instructor-approved setup and confirm only that configured path becomes available. | If the extraction count/recovery order, acidification endpoint, recovery method, or dryness state is invalid, revise the teacher setup or repeat that recovery stage before weighing; reset must clear recovered-mass/composition evidence but not invent component identity. | Require property-reference evidence, explicit plan approval, configured extraction/recovery execution, dried recovered-fraction masses, confirmed formula convention, and the final composition/recovery report calculations. |
| lab | `crystal-violet-rate-law` | Attempt one scientifically invalid or out-of-sequence operation specific to Rate Law of Crystal Violet Fading Using Beer's Law and verify the shared/custom runtime rejects it without committing evidence. | If a kinetics reading is attempted before wavelength/zero/calibration or at an invalid scheduled state, repair the instrument/reaction sequence and reacquire that time point; restart must clear the old series/regressions/order selection before a new run. | Require micromolar dilution evidence, spectrophotometer calibration, scheduled time-absorbance series, transformed regressions/order selection, any approved extension result, and completion of waste treatment. |
| lab | `acid-base-titration-curves` | Start without or with an invalid custom-investigation-config setup and verify the activity refuses before composition/play; then supply a valid instructor-approved setup and confirm only that configured path becomes available. | If the approved aliquot/increment/stability plan cannot produce a supported curve or a formal trial rejects a delivery/read branch, revise the plan or use the authored retry/disposal path and rerun that context; reset must clear old curve rows/landmark calculations/reflections. | Require approved-plan identity, recorded pH-volume rows for every selected context/replicate, equivalence/half-equivalence landmarks and unknown molarity/equilibrium calculations, plus particulate/uncertainty reflections and cleanup. |
| technique | `weighing` | Omit or invalidate one required slot (massMeasurementId) and verify standalone/composition materialization refuses instead of inventing a value. | If the mass cannot be acquired because the weighing vessel/balance state is wrong, place the correct vessel and repeat the balance read/record step; reset must remove the prior mass measurement and notebook record. | Require a balance-produced mass measurement under the configured measurement id and a notebook record tied to the same reading; a static configured mass must not satisfy completion. |
| technique | `measuring-volume` | Omit or invalidate one required slot (targetVolumeMl, volumeToleranceMl, volumeMeasurementId) and verify standalone/composition materialization refuses instead of inventing a value. | If the requested volume is outside the source/cylinder capacity or the wrong device/source is used, correct the configured target or apparatus and remeasure; reset must clear the old volume reading under its configured measurement id. | Require the measured cylinder volume under the configured measurement id, within the configured target/tolerance, plus the matching record step. |
| technique | `making-solution` | Omit or invalidate one required slot (initialSolventVolumeMl, soluteMassG, finalVolumeMl, …) and verify standalone/composition materialization refuses instead of inventing a value. | If initial/final volumes are physically inconsistent or solvent/solute transfer cannot complete, correct the teacher values or container state, repeat the failed addition/dissolution step, and restart without retaining the old solution observation. | Require completion of solvent/solute/final-volume operations and the configured solution observation; configuration alone must not count as prepared-solution evidence. |
| technique | `dilution` | Omit or invalidate one required slot (aliquotVolumeMl, finalVolumeMl, dilutionFactor, …) and verify standalone/composition materialization refuses instead of inventing a value. | If final volume is below aliquot volume or the configured factor disagrees with finalVolume/aliquot, correct the configuration before proceeding, remake the dilution, and reset the old dilution evidence/calculation. | Require recorded aliquot/final-volume execution evidence and the dilution-factor result consistent with finalVolume/aliquot under the configured evidence id. |
| technique | `transmittance-dilution` | Omit or invalidate one required slot (stockConcentrationM, stockConcentrationMeasurementId, stockVolumeMeasurementId, …) and verify standalone/composition materialization refuses instead of inventing a value. | If %T/decimal-T/absorbance calculation is rejected for missing or invalid recorded operands, reacquire the photometer reading or preceding dilution evidence and recalculate in order; reset must clear the dependent photometric calculation chain. | Require recorded stock/dilution operands and an actual %T measurement, then require decimal-transmittance/absorbance calculations to consume the correct recorded operand type. |
| technique | `transfer` | Omit or invalidate one required slot (transferVolumeMl, transferObservation) and verify standalone/composition materialization refuses instead of inventing a value. | If transfer is rejected for missing source contents, receiver overflow, or wrong source/target, select a valid source/receiver or smaller configured amount and repeat only the transfer; reset must restore pre-transfer contents/evidence. | Require a successful physical transfer that updates source/receiver contents plus the configured transfer observation; a notebook-only claim must not replace material movement. |
| technique | `hard-water-precipitation` | Omit or invalidate one required slot (precipitateMassG, finalVolumeMl) and verify standalone/composition materialization refuses instead of inventing a value. | If reagent transfer/final-volume state cannot produce the declared precipitate path, correct the configured quantities or source state and repeat the precipitation sequence; reset must remove prior precipitate/final-volume evidence. | Require reagent-transfer/precipitation evidence and the configured final-volume/precipitate evidence produced by the physical sequence. |
| technique | `filtration` | Omit or invalidate one required slot (filtrationMode, rinseVolumeMl) and verify standalone/composition materialization refuses instead of inventing a value. | If the filter assembly/receiver is missing, the target would overflow, or the mixture/rinse step is out of order, repair the apparatus and resume at that filtration step; reset must clear prior filtrate/retentate transfer evidence. | Require apparatus placement, mixture transfer through the filter path, configured rinse action, and resulting filtrate/retentate state/evidence before exit. |
| technique | `drying` | Omit or invalidate one required slot (firstDryingMinutes, secondDryingMinutes, ovenTemperatureC, …) and verify standalone/composition materialization refuses instead of inventing a value. | If a mass is taken before the required dry/cool sequence or repeat-drying tolerance is not met, return to heating/cooling and reweigh until the configured criterion is satisfied; reset must discard previous dry-mass iterations. | Require each authored dry/cool/weigh stage and the final mass/tolerance evidence under the configured measurement id before the technique can complete. |
| technique | `thermal-decomposition-mass-loss` | Omit or invalidate one required slot (sampleMassG, warmDurationMin, heatingDurationMin, …) and verify standalone/composition materialization refuses instead of inventing a value. | If tare/sample/heating/cooling evidence is incomplete or constant-mass criteria fail, repeat the bounded heat-cool-weigh cycle under the same tare convention; reset must clear prior cycle masses/composition and not switch conventions implicitly. | Require the selected tare-convention setup, initial/final mass readings across bounded heat-cool cycles, constant-mass satisfaction, and the composition calculation derived from measured mass loss. |
| technique | `paper-chromatography` | Omit or invalidate one required slot (selectedProcedure, baselineHeightMm, solventDepthMm, …) and verify standalone/composition materialization refuses instead of inventing a value. | If host-selected geometry/procedure is invalid or a paper/chamber step is out of order, correct the host setup or prepare a fresh selected trial and resume from its first valid node; reset must clear that trial's front/band/Rf evidence. | Require the host-selected procedure path, physical chamber/paper development sequence, recorded front/band distances, calculated Rf values, and disposal evidence for exactly the selected trials. |
| technique | `hard-water-calculation` | Omit or invalidate one required slot (sampleVolumeMeasurementId, precipitateMassMeasurementId, calculationId, …) and verify standalone/composition materialization refuses instead of inventing a value. | If named sample-volume or precipitate-mass evidence is missing, return to the producer step that owns that measurement/calculation, acquire it, and resubmit hardness; reset must remove the old hardness calculation. | Require the named sample-volume and precipitate-mass evidence records and a hardness calculation under the configured calculation id/tolerance that consumes those operands. |
| technique | `beers-law-calibration` | Omit or invalidate one required slot (stockVolumeMeasurementId, finalVolumeMeasurementId, wavelengthMeasurementId, …) and verify standalone/composition materialization refuses instead of inventing a value. | If a standard pair is missing or the calibration regression cannot be formed, reacquire/record the missing concentration-absorbance pair and rebuild the fit; reset must clear the old regression before accepting a new standard set. | Require the complete accepted concentration/absorbance standard pairs and the regression parameters calculated from those pairs; no authored slope/intercept may stand in for the fit. |
| technique | `brass-spectrophotometry` | Omit or invalidate one required slot (wavelengthMeasurementId, measurementCuvetteInstanceId) and verify standalone/composition materialization refuses instead of inventing a value. | If a scan/read is rejected because the photometer is not zeroed/configured or the proper cuvette is not inserted, complete that exact dark/blank/wavelength/insertion prerequisite and reread; reset must clear stale scan/calibration/unknown evidence. | Require valid zero/wavelength/cuvette state for every scan/calibration/unknown read, distinct read/record evidence, and concentration/copper calculations based on the accepted calibration/unknown measurements. |
| technique | `hard-water-gravimetry` | Omit or invalidate one required slot (ovenTemperatureC, firstDurationMinutes, coolingTemperatureC) and verify standalone/composition materialization refuses instead of inventing a value. | If sample volume, precipitate mass, or drying evidence is incomplete, return to the named acquisition/derivation step, produce the missing evidence, and recalculate; reset must remove the previous gravimetric mass/hardness chain. | Require named sample-volume, precipitate mass/combined-tare derivation, and resulting hardness evidence, with every derivation consuming recorded upstream measurements. |
| technique | `titration-endpoint` | Omit or invalidate one required slot (endpointWindowMl, persistenceSeconds, maximumIncrementMl, …) and verify standalone/composition materialization refuses instead of inventing a value. | If the endpoint path is rejected for missing initial reading, preparation, or persistence/appearance requirement, restore the burette/analyte state and repeat delivery/read/record from the last valid node; reset must clear prior endpoint/trial calculations. | Require mounted/conditioned burette state, separate initial/final readings, accepted persistent endpoint evidence, and the final titration calculation from those readings. |
| technique | `bonding-solids-tests` | Omit or invalidate one required slot (sampleIdentity, sampleMode, evidenceScopeId, …) and verify standalone/composition materialization refuses instead of inventing a value. | If an aqueous/reagent/solvent/melting test is attempted without its required prepared sample state or outside the host-selected procedure, return to the preparation step for that test and repeat it without revealing blind identity; reset clears that sample's test evidence. | Require evidence for every test selected by the host procedure—prepared portion, test application/read/record—and preserve blind-redaction metadata through the final classification evidence. |
| technique | `redox-titration` | Omit or invalidate one required slot (endpointWindowMl, persistenceSeconds, maximumIncrementMl, …) and verify standalone/composition materialization refuses instead of inventing a value. | If standardization/sample calculation is attempted with missing burette/aliquot evidence or replicate spread exceeds the approved range, reacquire/repeat the affected trial and recompute the standardized titrant before sample analysis; reset clears both trial and derived records. | Require standard aliquot and burette evidence for each standardization trial, accepted mean/concordance, sample aliquot/readings, and the final peroxide/redox calculation derived from the standardized concentration. |
| technique | `tablet-separation` | Attempt one scientifically invalid or out-of-sequence operation specific to Tablet Separation and verify the shared/custom runtime rejects it without committing evidence. | If an extraction/filtration/recovery mass is missing or a component-percent calculation lacks its starting/recovered mass, return to the specific separation/recovery/weigh step, reacquire the mass, and recalculate; reset removes old recovered-fraction evidence. | Require physical separation/recovery evidence, named starting/recovered masses, and component-percent/total-recovery calculations derived from those masses. |
| technique | `crystal-violet-kinetics` | Omit or invalidate one required slot (selectedProcedure, cvVolumeMl, naohVolumeMl, …) and verify standalone/composition materialization refuses instead of inventing a value. | If a scheduled absorbance read lacks wavelength/zero state, reaction initiation, or its paired time record, restore the instrument/reaction state and reacquire that point; reset must clear the time-absorbance series used by later regressions. | Require reaction initiation evidence plus paired recorded scheduled-time and absorbance values under valid instrument state, producing the series consumed downstream. |
| technique | `hand-warmer-calorimetry` | Omit or invalidate one required slot (wasteRoute) and verify standalone/composition materialization refuses instead of inventing a value. | If the lab-scoped procedure lacks waste configuration, mass, timer, or temperature evidence, repair the exact configuration/acquisition step and repeat that branch; reset must clear calorimetry measurements/calculations while preserving no hidden teacher result. | Require lab-scoped teacher waste configuration, physical balance/timer/temperature evidence, calibration and trial calculations, and the final inquiry/design evidence outputs declared by the technique. |
| technique | `titration-curve-analysis` | Do not expect refusal when `analysisMode` is omitted: its approved `recorded-evidence-only` default is applied. Instead attempt `calculate-curve-molarity` without recorded `curve-analyte-volume` or `equivalence-volume` and verify the reducer refuses; after both measurements are recorded, the calculation may proceed. | If molarity calculation is attempted without recorded `curve-analyte-volume` or `equivalence-volume`, return to measurement/curve analysis to record those values and resubmit; reset must clear the previous curve-molarity calculation. Omitting `analysisMode` alone is not a refusal because its approved `recorded-evidence-only` default is applied. | Require recorded `curve-analyte-volume`, initial-pH/equivalence evidence, and `curve-molarity` computed from the recorded aliquot/equivalence plus model titrant/stoichiometry; the defaulted `analysisMode` is configuration, not completion evidence. |
| technique | `blue1-standard-dilutions` | Attempt one scientifically invalid or out-of-sequence operation specific to Blue #1 Standard Dilutions and verify the shared/custom runtime rejects it without committing evidence. | If a standard's stock/final-volume evidence is missing or a dilution volume is invalid, correct/remake that standard and rerecord its dilution evidence; reset must clear only the old standard-preparation evidence. | Require each standard's measured/configured stock/final-volume preparation evidence and its derived concentration/dilution record before that standard is available to calibration. |
| technique | `blue1-percent-transmittance` | Omit or invalidate one required slot (calibrationRegressionCalculationId) and verify standalone/composition materialization refuses instead of inventing a value. | If %T is read/recorded before the photometer is configured/zeroed or the sample cuvette is properly inserted, perform those prerequisites and reacquire the instrument value; reset clears old %T/derived photometric records. | Require instrument configuration/zero/cuvette insertion, an actual %T read, a distinct recorded %T entry, and any downstream decimal-T/absorbance result consuming that record. |
| technique | `blue1-class-calibration` | Omit or invalidate one required slot (i1-r10-0-concentrationCalculationId, i1-r8-2-concentrationCalculationId, i1-r6-4-concentrationCalculationId, …) and verify standalone/composition materialization refuses instead of inventing a value. | If the class calibration lacks enough accepted standard pairs or includes an unconfirmed pair, correct the class evidence set and recompute the regression; reset removes the prior fit without rewriting original standard evidence. | Require the accepted set of class standard concentration/absorbance records and the regression derived from those accepted pairs, preserving source-pair provenance. |
| technique | `hard-water-practice-preparation` | Attempt one scientifically invalid or out-of-sequence operation specific to Hard-Water Practice Preparation and verify the shared/custom runtime rejects it without committing evidence. | If practice reagent/apparatus preparation is incomplete or out of order, return to the missing preparation step and complete it before the gravimetric handoff; reset clears the practice setup evidence so the preparation can be repeated cleanly. | Require completion/recording of the declared practice reagent/apparatus preparation outputs that gate the later hard-water gravimetric path. |
| technique | `gravimetric-vacuum-filtration` | Attempt one scientifically invalid or out-of-sequence operation specific to Gravimetric Vacuum Filtration and verify the shared/custom runtime rejects it without committing evidence. | If vacuum apparatus attachment, transfer, or rinse state is invalid, correct the flask/Büchner/filter/vacuum setup and repeat the affected filtration step; reset must clear collected filtrate/solid transfer evidence. | Require assembled vacuum apparatus evidence, physical mixture/rinse transfers, and the filtered precipitate/receiver state needed by downstream drying. |
| technique | `two-stage-precipitate-drying` | Omit or invalidate one required slot (ovenTemperatureC, firstDurationMinutes, coolingTemperatureC) and verify standalone/composition materialization refuses instead of inventing a value. | If teacher oven/time/cooling values are absent/invalid or the second-stage mass criterion is not met, correct the setup or repeat dry-cool-weigh until valid; reset clears stage-specific masses/temperature evidence. | Require instructor oven/time/cooling configuration plus first/second dry-cool-weigh records and the configured endpoint/mass criterion before exit. |
| technique | `inquiry-plan-approval` | Attempt one scientifically invalid or out-of-sequence operation specific to Inquiry Plan Approval and verify the shared/custom runtime rejects it without committing evidence. | If the inquiry plan is incomplete or teacher approval has not been recorded, return to the design/safety fields, revise them, and resubmit for approval; reset clears the approval record so a changed plan cannot inherit an old approval. | Require the learner's plan/safety evidence and a distinct teacher approval record bound to that plan version; approval is the completion gate. |
| technique | `hard-water-two-sample-inquiry` | Omit or invalidate one required slot (ovenTemperatureC, firstDurationMinutes, coolingTemperatureC) and verify standalone/composition materialization refuses instead of inventing a value. | If either sample lacks configured drying/cooling or named mass/volume evidence, repair/repeat only that sample's affected stage and recalculate it; reset must clear both samples' derived hardness comparison while preserving sample identity separation. | Require sample-specific volume/filtration/dry-mass evidence for both waters under the instructor setup and separate hardness results before comparative interpretation. |
| technique | `beverage-ph-volume-titration` | Omit or invalidate one required slot (endpointWindowMl, persistenceSeconds, maximumIncrementMl, …) and verify standalone/composition materialization refuses instead of inventing a value. | If a beverage trial lacks configured preparation/aliquot, valid burette/pH evidence, or an accepted quantitative branch, repair that trial's setup/read sequence and rerun it; reset clears old curve/equivalence/calculation records for that trial. | Require configured beverage trial identity, physical pH/burette row acquisition with read/record provenance, and the quantitative endpoint/equivalence/calculation evidence for the accepted trial. |
| technique | `crystal-violet-micromolar-dilution-series` | Attempt one scientifically invalid or out-of-sequence operation specific to Micromolar Crystal Violet Dilution Series and verify the shared/custom runtime rejects it without committing evidence. | If a micromolar standard has missing stock/final-volume inputs or an invalid dilution relationship, correct/remake that standard and recalculate its concentration; reset clears the old concentration-series preparation evidence. | Require source/final-volume preparation records and derived micromolar concentration evidence for every standard used by calibration. |
| technique | `crystal-violet-spectrophotometer-calibration` | Attempt one scientifically invalid or out-of-sequence operation specific to Crystal Violet Spectrophotometer Calibration and verify the shared/custom runtime rejects it without committing evidence. | If calibration is attempted before the configured wavelength/blank/standard reads are complete, finish the missing zero/read step and rebuild the calibration; reset clears the old instrument-calibration evidence before a new kinetics run. | Require configured wavelength, zero/blank/standard instrument evidence and the resulting accepted calibration record before any kinetics read can count. |
| technique | `crystal-violet-integrated-rate-law-comparison` | Omit or invalidate one required slot (selectedProcedure) and verify standalone/composition materialization refuses instead of inventing a value. | If a regression/order calculation lacks the concentration-time series or one transformed fit, return to the missing upstream calculation/series, regenerate it, and rerun the comparison; reset clears prior regressions/order/k values. | Require the recorded concentration-time series, all configured transformed regressions, their R² evidence, evidence-backed order selection, and any rate-constant/extension calculation. |
| technique | `crystal-violet-waste-treatment` | Attempt one scientifically invalid or out-of-sequence operation specific to Crystal Violet Waste Treatment and verify the shared/custom runtime rejects it without committing evidence. | If waste treatment is attempted before the kinetics material is in the declared cleanup state or the disposal sequence is out of order, restore the correct waste receiver/material state and repeat cleanup; reset clears cleanup evidence without recreating analytical data. | Require the physical cleanup/waste-transfer actions and destination evidence for the post-kinetics material before the treatment technique exits. |
| technique | `quick-ache-property-evidence` | Attempt one scientifically invalid or out-of-sequence operation specific to Quick Ache Component Property Evidence and verify the shared/custom runtime rejects it without committing evidence. | If a property test is attempted with the wrong reference portion/reagent or before the test portion is prepared, prepare the correct portion and repeat that test; reset clears the property observations while preserving no inferred recovered identity. | Require the prepared reference test portions and recorded property-test results for each selected pure component; no recovered-fraction identity claim is an evidence substitute. |
| technique | `quick-ache-design-approval` | Attempt one scientifically invalid or out-of-sequence operation specific to Quick Ache Separation Design and Approval and verify the shared/custom runtime rejects it without committing evidence. | If the separation flow/safety audit is incomplete or teacher approval is absent, revise the plan and obtain a new approval; reset must revoke the old approval so a changed procedure cannot proceed under stale authorization. | Require a recorded separation-flow design, safety audit, and explicit teacher approval tied to that design before extraction can proceed. |
| technique | `quick-ache-extraction-recovery` | Omit or invalidate one required slot (organicRecoveryMethod, aqueousRecoveryMethod, drynessCriterion, …) and verify standalone/composition materialization refuses instead of inventing a value. | If extraction count/order, acidification endpoint, drying/recovery approval, or fraction state is invalid, correct the teacher configuration or repeat the affected extraction/recovery stage before weighing; reset clears fraction/recovered-mass evidence. | Require configured recovery approval/procedure, physical extraction/fraction/recovery/drying actions, and recorded recovered masses for every required fraction. |
| technique | `quick-ache-analysis-report` | Attempt one scientifically invalid or out-of-sequence operation specific to Quick Ache Composition Analysis and verify the shared/custom runtime rejects it without committing evidence. | If composition/recovery calculations lack the confirmed formula convention or named starting/recovered masses, return to the confirmation/weighing producer, record the missing evidence, and recalculate; reset removes old report calculations but not source measurements. | Require confirmed calculation convention plus named starting/recovered-mass evidence and the component-percent/total-recovery calculations/report derived from those values. |
| technique | `marble-gas-syringe-kinetics` | Omit or invalidate one required slot (selectedProcedure, acidVolumeMl, acidVolumeToleranceMl, …) and verify standalone/composition materialization refuses instead of inventing a value. | If marble mass, acid volume/concentration, gas-syringe connection, or 0/15-s gas readings are missing, repair/repeat that trial setup and reacquire the timed series; reset clears the old series and initial-rate result. | Require measured marble mass, prepared acid/trial identity, recorded gas-volume time series including required early points, and the initial-rate calculation from that series. |
| technique | `equilibrium-rainbow-inquiry` | Attempt one scientifically invalid or out-of-sequence operation specific to Equilibrium Rainbow Inquiry and verify the shared/custom runtime rejects it without committing evidence. | If an unsupported stress or unprepared equilibrium state is used, restore the declared baseline/sample condition and reapply one supported stress before rerecording; reset clears the prior stressed observation/explanation. | Require baseline identity, approved stress application, qualitative observation record, and the learner's interpretation/reflection for the selected inquiry path. |
| technique | `ph-volume-formal-titration-trial` | Omit or invalidate one required slot (contextId, formalContextModelId, aliquotMl, …) and verify standalone/composition materialization refuses instead of inventing a value. | If an increment violates configured bounds, a pH/burette read is missing, or the retry branch is selected, follow the authored dispose-rinse-reset chain and restart that formal context from its valid trial state; reset clears old row/reading evidence. | Require apparatus/setup state, initial burette/pH row, every accepted delivery-mix-read-record decision row, final burette record, and required disposal/cleanup evidence for the selected context. |
