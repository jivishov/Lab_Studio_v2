# Export provenance

This repository is an independent private GitHub repair checkout containing a source snapshot of Lab Studio. It was copied from the authoritative live working tree at `C:\Users\EmilJivishov\Projects\Lab_studio`, which is a `Lab_studio/` subtree of the parent Git root `C:\Users\EmilJivishov\Projects`.

The source was on branch `codex/Lab_Studio` at `c32ba48e30cb8b8a86438e131bd7c6f146487fae` at export preflight. The working tree was intentionally dirty; the export used current files from that working tree rather than a Git archive or a clean HEAD checkout. The erroneous `Projects\Lab\_studio` path was not used.

The detailed source and destination manifests, copy report, and final verification JSON are kept with the local export evidence outside this repository. They record relative paths, SHA-256 hashes, byte sizes, and the source Git/dirty provenance. Every selected application/source file is expected to match the final source manifest byte-for-byte. The only deliberate content adaptations are the root `AGENTS.md` and `README.md` files; the original README is preserved at [SOURCE_README.md](SOURCE_README.md).

The four open-chamber PNG/SVG assets under `public/assets/equipment-realistic/v1/` were copied from the live source. Required catalogs, realistic equipment art, MediaPipe models, and WASM files remain under `public/`.

This export does not repair the application and does not promote it to release, runtime, scientific, classroom, or human-validation readiness. The independent repair review is intentionally not packaged here; the user will provide it separately to the future web ChatGPT repair worker.
